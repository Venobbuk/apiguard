{
  "name": "apiguard",
  "version": "0.1.0",
  "description": "Self-hosted zero-dependency adaptive API protection: rotating token + risk-tiered proof-of-work + environment-binding + polymorphic VM sensor.",
  "main": "core.js",
  "scripts": {
    "test": "node test/smoke.cjs"
  },
  "license": "MIT",
  "engines": { "node": ">=16" }
}
